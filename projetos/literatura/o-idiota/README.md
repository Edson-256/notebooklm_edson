# Projeto: O Idiota

- **Fonte:** Fiódor Dostoiévski (EPUB)
- **Origem:** mbookpro (`~/Downloads`), baixado 2026-06-05
- **Status:** ✅ concluído
- **Capítulos:** 51 em `output/chapters/`
- **Método:** `python ../../../tools/convert_epub.py --write --level 3`

4 partes × capítulos = 50 caps (H3) + FrontMatter.

## Reprocessar

De dentro desta pasta, com o venv ativo (`source ../../../.venv/bin/activate`):

```bash
python ../../../tools/convert_epub.py --write --level 3
```

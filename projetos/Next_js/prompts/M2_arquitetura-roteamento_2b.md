<role>
You are a Podcast Producer and Expert Technical Storyteller.
</role>

<context>
We are creating an engaging audio script for Module 2 of a Next.js course for beginners, using "The Architect's Blueprint" as the source material. The focus is the App Router, file-system-based routing, and nested layouts. This script will accompany the previously generated presentation slides.
</context>

<instructions>
Think step-by-step in English to structure the narrative arc and maximize semantic density, then translate your final output:
1. Hook the listener by introducing the transition from traditional routing to Next.js's intuitive file-system routing.
2. Explain the App Router using a clear, real-world analogy: folders are like rooms in a building, and the `page.js` file is the unique furniture inside that room.
3. Explain nested layouts (`layout.js`) simply. Highlight the "persistent sidebar" example to show how it preserves UI state without reloading the whole page.
4. Briefly introduce Dynamic Routes (e.g., `[slug]`) as a "placeholder" or a "door with a blank nametag" for dynamic content like blog posts.
5. Structure the output as a continuous, flowing script. Include subtle vocal cues in brackets (e.g., *[Pausa curta]*, *[Tom entusiasmado]*) to guide the pacing of the audio generation.
</instructions>

<constraints>
- Do NOT use raw code syntax or literal file paths (e.g., say "o arquivo de página" instead of "page dot j-s") to avoid confusing the text-to-speech engine.
- Do NOT use heavy technical jargon without an immediate, simple analogy.
- Do NOT sound robotic; maintain a warm, pedagogical, and conversational tone.
- Be highly concise to mitigate the higher token costs associated with the Portuguese language, keeping the text suitable for about 3 minutes of spoken audio.
</constraints>

<output_format>
Ensure your final response is written entirely in natural, fluent, spoken Brazilian Portuguese (PT-BR).
</output_format>
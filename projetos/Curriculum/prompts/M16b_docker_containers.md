<role>
Act as a Podcast Producer and Technical Storyteller.
</role>
<context>
Deep-dive audio for Module 16: Docker containers and Docker Compose for development environments. Accompanies slides about containerization, the pgvector PostgreSQL image, and reproducible dev setups.
</context>
<instructions>
Think step-by-step:
1. Open with: "It works on my machine — the most dangerous phrase in software."
2. Explain containers as "lightweight virtual machines that boot in seconds."
3. Walk through a real scenario: spinning up PostgreSQL 16 with pgvector in one command for local development.
4. Cover the reset workflow: `docker compose down -v` to start fresh, `docker compose up -d` to rebuild.
5. Write a conversational script (approx. 3-4 minutes).
</instructions>
<constraints>
- Warm, pedagogical tone. Flowing paragraphs.
- Be highly concise to fit standard audio lengths.
</constraints>
<output_format>
Audio format: deep_dive
Ensure your final response is written in natural, spoken Brazilian Portuguese (PT-BR).
</output_format>

<role>
Act as an Expert Technical Educator and Presentation Designer.
</role>
<context>
Module 4: Advanced Tailwind CSS. Covers `tailwind.config.ts` customization, design tokens (colors, spacing scales, fonts), the `@apply` directive, CSS variables integration, dark mode strategies (class-based vs media query), and creating a consistent design system.
</context>
<instructions>
Think step-by-step:
1. Explain design tokens as "the DNA of your visual identity" — change once, update everywhere.
2. Show how `tailwind.config.ts` extends the default theme.
3. Cover dark mode implementation: the `dark:` prefix and class-based toggling.
4. Structure as slide deck outline with configuration code snippets in Speaker Notes.
</instructions>
<constraints>
- Assume the learner completed M3 (utility-first basics).
- Keep config snippets minimal (5-7 lines).
- Be concise to mitigate token costs.
</constraints>
<output_format>
Ensure your final response is formatted in Markdown and written in fluent, concise Brazilian Portuguese (PT-BR).
</output_format>
